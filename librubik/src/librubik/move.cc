#include "move.hh"

namespace rubik
{
    /*
        std::vector<Move> parse_moves(std::string input){
            std::vector<Move> res;
            for(auto i = input.begin(); i != input.end(); i++){

                if (regexp_match())

                switch(i){
                case [a-zA-Z]:
                    qwe;
                case "\'":
                    ewq;
                case ' ':
                    break;
                }
            }
        }
    */
    // FIXME

} // namespace rubik
